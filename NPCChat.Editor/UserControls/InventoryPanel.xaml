<UserControl x:Class="NPCChat.Editor.UserControls.InventoryPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignWidth="200" d:DesignHeight="300"
             Visibility="Collapsed">

    <Border Background="#CC0D1B2A"
            BorderBrush="#334155"
            BorderThickness="1"
            CornerRadius="8"
            Padding="12,10"
            MinWidth="180">
        <StackPanel>

            <!-- Header -->
            <TextBlock Text="Inventory"
                       FontSize="12"
                       FontWeight="SemiBold"
                       Foreground="#F59E0B"
                       Margin="0,0,0,8" />

            <!-- Slot list -->
            <ItemsControl x:Name="SlotsList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,1,0,1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0"
                                       Text="{Binding Name}"
                                       FontSize="11"
                                       Foreground="#CBD5E1" />
                            <TextBlock Grid.Column="1"
                                       Text="{Binding Quantity}"
                                       FontSize="11"
                                       Foreground="#64748B"
                                       Margin="8,0,0,0" />
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- Empty message -->
            <TextBlock x:Name="EmptyText"
                       Text="(empty)"
                       FontSize="11"
                       Foreground="#475569"
                       Visibility="Collapsed" />

            <!-- Hint -->
            <TextBlock Text="I to close"
                       FontSize="10"
                       Foreground="#475569"
                       Margin="0,8,0,0" />

        </StackPanel>
    </Border>
</UserControl>
