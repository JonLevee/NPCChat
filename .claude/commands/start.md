Orient yourself for this work session by doing the following steps silently, then give a single concise briefing.

## Steps

1. Read `PLANNING.md` from the project root.
2. Run `git status` and `git log --oneline -10` to see the current branch, uncommitted changes, and recent commits.
3. Check the auto-memory index (MEMORY.md in the project memory directory — path is in the system prompt) for any entries relevant to the current phase.

## Output

Print a single briefing in this format:

**Branch:** [current branch]
**Phase:** [current phase and goal from PLANNING.md]

**Last session accomplished:**
- [bullet points from PLANNING.md "Completed this session"]

**Up next:**
1. [first next step]
2. [second next step]
3. [third next step]

**Anything needing a decision before we start:** [or "None" if there isn't]

Then ask: "Ready to continue, or is there anything to update first?"
