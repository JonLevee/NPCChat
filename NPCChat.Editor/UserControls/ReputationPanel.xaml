<UserControl x:Class="NPCChat.Editor.UserControls.ReputationPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignWidth="220" d:DesignHeight="200"
             Visibility="Collapsed">

    <Border Background="#CC0D1B2A"
            BorderBrush="#334155"
            BorderThickness="1"
            CornerRadius="8"
            Padding="12,10"
            MinWidth="180">
        <StackPanel>

            <!-- Header -->
            <TextBlock Text="Reputation"
                       FontSize="12"
                       FontWeight="SemiBold"
                       Foreground="#C084FC"
                       Margin="0,0,0,8" />

            <!-- Faction list -->
            <ItemsControl x:Name="FactionList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,2,0,2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0"
                                       Text="{Binding FactionName}"
                                       FontSize="11"
                                       Foreground="#CBD5E1" />
                            <TextBlock Grid.Column="1"
                                       Text="{Binding Tier}"
                                       FontSize="10"
                                       Foreground="{Binding TierColor}"
                                       Margin="8,0,6,0" />
                            <TextBlock Grid.Column="2"
                                       Text="{Binding Score}"
                                       FontSize="10"
                                       Foreground="#64748B" />
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- Hint -->
            <TextBlock Text="R to close"
                       FontSize="10"
                       Foreground="#475569"
                       Margin="0,8,0,0" />

        </StackPanel>
    </Border>
</UserControl>
